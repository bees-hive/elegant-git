architect: extsoft
docker:
    image: beeshive/elegant-git-ci:2
    as_root: true
merge:
    fast-forward: only
    rebase: true
    script: ./run-tests
release:
    script: |-
        echo "Release ${tag}"
        bats --tap src/test
